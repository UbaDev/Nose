import { form } from './Form';
import { StyleSheet } from 'react-native';
export const globalStyles = StyleSheet.create({
    input: {
        marginBottom: 20,

    },
    buttonSubmit: {
        padding: 5,
    },

    buttonText: {
        marginTop: 10,
        
    }
})